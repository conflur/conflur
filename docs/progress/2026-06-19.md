# Sesión 2026-06-19

## Hecho

### SEB-164 — Railway + Vercel (✅ Done)
- Backend en Railway: `https://conflur-production.up.railway.app` (root `backend`, start `python main.py`).
- Frontend en Vercel bajo team `empresas-ia` (Hobby, repo público).
- Errores resueltos (ver Linear SEB-164 + memoria `reference_deploy_railway_vercel`): `$PORT` no se expande en Railpack (5 lugares con `$PORT` literal); `next.config.ts` no soportado en Vercel (→ `.mjs` JS puro).

### SEB-166 — Auth (🟡 In Progress) — fundación de tenancy
Antes de codear auth se rediseñó el modelo de tenancy (el schema 1:1 previo era deuda):

- **D11** Auth = estándar de plataforma (no elección de instancia); verificación en FastAPI. Patrón `token → tenant_id → set_tenant(RLS)` documentado en `platform-boundary.md`.
- **D12** Tenant de primera clase: `tenants` + `memberships` + roles (owner/professional/assistant). Suscripción a nivel consultorio.
- **D13** Visibilidad clínica vía `patient_access` (RLS entre consultorios + autorización adentro). Interconsulta = compartir explícito/revocable/temporal/auditado.
- **D14** Rol de app `conflur_app` SIN bypassrls (`APP_DATABASE_URL`); `neondb_owner` solo migraciones. Se detectó que `neondb_owner` saltea RLS aunque esté FORCE.

Construido: 3 modelos nuevos (tenant, membership, patient_access) + 7 actualizados; migración 0001 reescrita y aplicada a Neon (12 tablas, RLS en 8); `set_tenant` con `set_config()` + `app.user_id`. **9 tests pasan**, incluido aislamiento RLS real con rol sin bypass.

Infra: rol `conflur_app` creado en Neon; `APP_DATABASE_URL` cargada en Railway y backend redeployado OK.

## Pendiente / próximo
- **Resto de SEB-166:** endpoints `/auth/*` en FastAPI (password con passlib + WebAuthn con `py_webauthn`), NextAuth Credentials+JWT en frontend, middleware que valida JWT → `set_tenant`. Falta agregar `py_webauthn` a requirements.
- **Migrar Neon `conflur`** de `sebabizzi` a `empresas.ia.dev` (pendiente arrastrado).
- Crear API key Anthropic bajo `empresas.ia.dev` (al llegar a SEB-165 LiteLLM).

## Próxima sesión sugerida
Terminar SEB-166: implementar el flujo de auth (registro/login email+password primero, luego passkeys), con el middleware de `set_tenant` y tests de auth en pytest. Considerar SEB-165 (LiteLLM) en paralelo si se quiere destrabar SEB-169 (notas IA).
