# Progress — 2026-07-05 — Auditoría de cumplimiento de plataforma

## Qué se hizo

Sesión de auditoría iniciada desde el contexto de plataforma (no desde una sesión propia de
eia1) — se audita esta instancia sin tocar su código, como parte de una auditoría de cumplimiento
de toda la plataforma. Ver `../docs/AUDITORIA_CUMPLIMIENTO_2026-07-05.md` para el detalle completo
con evidencia archivo:línea.

## Hallazgos propios de esta instancia (E1-E12)

**Críticos, a confirmar hoy (no necesariamente bugs, pero de máxima prioridad):**
- **E1 (SEB-254):** confirmar si Railway producción usa el rol `conflur_app` o si sigue en
  `neondb_owner` (saltearía el RLS) — ya estaba flageado en `agents.md`, sigue sin confirmación.
- **E2 (SEB-255):** confirmar que no haya pacientes reales con notas clínicas cargadas — el
  contenido se almacena en texto plano hasta que se construya el cifrado (D22, ya diseñado).

**Altos:** sin headers de seguridad en el backend (E3); expiración de sesión por inactividad
documentada pero no implementada (E4); Stripe/MercadoPago sin webhooks reales (E5); sin logging
de requests/errores (E6).

**Medios:** Design Gate describe tenancy pre-rediseño, el código real ya está bien (E7);
`APP_ENV=development` en prod causa logging de SQL completo (E8); agente de notas sin KM, deuda
M1 ya documentada (E9).

**Bajos:** Discovery público sin rate limiting (E10); nitpicks de `.env.example` (E11) y de
tests de RLS (E12).

## Qué cumple bien (para no perder de vista en medio de la lista de gaps)

RLS con `FORCE ROW LEVEL SECURITY` + fail-closed + test real; autorización por recurso genuina
(404 en vez de 403); tenancy de primera clase implementada correctamente en código.

## Qué quedó pendiente

Revisar cada hallazgo con Sebas, empezando por E1 y E2. Ninguno se resolvió en esta sesión.
