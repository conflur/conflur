# Sesión 2026-07-01 — Capa profesional + primer agente que habla con gente

## 🔜 PRÓXIMA SESIÓN
**Agente de Descubrimiento — Fase 3: canal Telegram** (consentimiento + salida a humano), que llama a
`discovery_reply` (turnos) → al cierre `synthesize_discovery` + `save_finding`. Antes, re-correr el sim
para confirmar que la lección (no repetir preguntas) se aplicó.

## Hecho hoy

### 1. Reencuadre + capa profesional (diseño)
- Vender **alivio del dolor de gestionar el consultorio**, no un sistema. Tool + formación.
- `docs/capa-profesional.md` (nav, "Hoy" por rol, "Mis números" con separación consultorio/personal,
  formación en 4 capas + KM). Backlog: SEB-194/195/199/200/201/202.

### 2. Comunicación de agentes (Capa 0-1) — SEB-210
- `docs/comunicacion-agentes.md`: criterios fundados en **Grice** + investigación UX, anti-patrones,
  disclosure oportuna, **banco de ejemplares** (sembrado con las correcciones de Sebas) y **loop de
  aprendizaje** (señal → lección → KM). "Cada corrección entrena a todos los agentes."

### 3. Agente de Descubrimiento — CONSTRUIDO (primer agente que habla con gente)
- **Fase 1 — motor conversacional** (`backend/agents/discovery.py`): compone el prompt desde
  criterios+flujo+config (`DiscoveryConfig`), produce un turno. Capa 0-1 (motor) + config por instancia.
  Verificado con LLM real: produce la apertura acordada.
- **Loop en acción:** corrida simulada completa → cazó repetición de preguntas → lección capturada
  (criterio + ejemplar E4). Primer ciclo del loop girado.
- **Fase 2a — síntesis** (`synthesize_discovery`): transcript → JSON estructurado. Verificado real
  (dato: la psicóloga llamaría "finanzas" a la sección, no "Mis números" → revisar SEB-195).
- **Fase 2b — persistencia + consolidación** (`DiscoveryFinding` migr 0013 RLS + `discovery_store.py`):
  guarda por charla en la DB de la instancia (aislado; NO en la KM de CRIZA) + consolida across charlas.
- Tests: 5 unit (prompt, reply, apertura, síntesis, consolidación).

## Estado agente: Fase 1 ✅ · 2a ✅ · 2b ✅ · 3 (canal) pendiente.

## Housekeeping de Sebas (sin apuro)
Borrar tokens Railway/Vercel; decomisar deploys viejos de empresas-ia.
