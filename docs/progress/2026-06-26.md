# Sesión 2026-06-26 — RETOMAR ACÁ (pausa por límite de uso)

> Esta sesión se cortó por límite semanal de tokens. Todo lo necesario para
> continuar sin re-pensar está acá. Mañana = ejecución directa.

## Modelo definitivo de cuentas (ESTABLE — no volver a discutir)

- **Una sola identidad humana de GitHub: `sebasbizzi`**, Owner de TODAS las orgs (`conflur`, `CRIZA-ia`, `empresas-ia`). Las cuentas-bot (`empresas-ia-dev`, `criza-dev`) se retiran.
- **Deploys (Vercel/Railway) se conectan a GitHub vía `sebasbizzi`** (es la única identidad que ve todas las orgs de instancia sin ensuciarlas con bots).
- **NO una cuenta de Railway/Vercel por instancia** (multiplica costo). Se usa **UNA cuenta de operación conectada a `sebasbizzi`**, con **un proyecto por instancia** adentro. Railway ≈ $5/mes base total; Vercel Hobby gratis.
- **Unidad de venta = el PROYECTO** (Vercel/Railway permiten transferirlo), + org de GitHub + base Neon + dominio + email de la instancia. La cuenta de operación se queda.
- **Cuentas personales** (sebabizzi@gmail) con `sinap`/`consultorios-financieros` = proyectos propios, NO del holding. Quedan aparte.
- Las cuentas Vercel/Railway linkeadas a **empresas.ia.dev** son un callejón sin salida (no ven las orgs nuevas) → **se decomisan**.

## Estado GitHub (HECHO ✅)

- `conflur/conflur` = repo vivo (migrado por **espejo/mirror**, no Transfer, por hold anti-fraude de GitHub). `empresas-ia/conflur` quedó **archivado**.
- `CRIZA-ia/scientific` = repo de CRIZA en su org propia (movido por Sebas).
- Remote local de Conflur ya apunta a `conflur/conflur`; push verificado.

## Reconexión Vercel/Railway — EN CURSO, retomar acá

> **Cuenta a usar:** la de Vercel/Railway conectada a **`sebasbizzi`** (la que muestra
> orgs sebasbizzi/empresas-ia/sinap-io). Las de empresas.ia.dev NO sirven (no ven la org `conflur`).

**Dar acceso a la org `conflur` — IMPORTANTE: NO usar URLs `github.com/apps/.../installations/new`
(dan 404). Se hace desde el botón DENTRO de Railway/Vercel**, que abre la página real de GitHub:
- **Railway:** New Project → "GitHub Repository" → como `conflur/conflur` no aparece, clic en
  **"Configure GitHub App"** / "Edit" → elegir org **`conflur`** → dar acceso → Install.
- **Vercel:** pantalla de Import → botón **"Adjust GitHub App Permissions"** → org `conflur` → acceso.

**Carga de variables = importar el `.env` (NO tipear a mano):**
- **Railway (backend):** servicio → **Root Directory = `/backend`** → pestaña **Variables → "Raw Editor"**
  → pegar TODO el contenido de `backend/.env` local (ya tiene todas las keys; no hay que conseguir
  ninguna) → guardar → agregar/ajustar **`FRONTEND_URL`** = URL del front en prod → **Deploy** →
  Settings → Networking → **Generate Domain** = la URL pública del backend.
- **Vercel (frontend):** **Root Directory = `frontend`**. NO importar el `.env` del backend (tiene
  secretos del back). Solo 3 vars: `NEXTAUTH_SECRET` (mismo valor que Railway), `NEXTAUTH_URL`
  (URL del front), `NEXT_PUBLIC_API_URL` (URL del back de Railway).

**Las 3 URLs cruzadas:** `FRONTEND_URL`(Railway) = `NEXTAUTH_URL`(Vercel) = URL del front ·
`NEXT_PUBLIC_API_URL`(Vercel) = URL del back · `NEXTAUTH_SECRET` idéntico en ambos.
Orden: deploy Railway → copiar URL back → deploy Vercel → copiar URL front → completar las cruzadas
→ redeploy. (Si se usa `conflur.com` desde el inicio, se evita el redeploy.)

**Después:** verificar front+back, fix 🔴 `APP_DATABASE_URL` (rol sin bypassrls), **decomisar**
Conflur de las cuentas empresas.ia.dev.

### Estado emocional / proceso (importante para retomar)
Sebas quedó **insatisfecho** con cómo viene: demasiados tokens gastados en desenredar el lío de
cuentas (originado en recomendaciones de identidad inconsistentes de Claude) y con instrucciones de
Claude que tuvieron huecos y un link 404. **Al retomar: ser claro, concreto y paso-a-paso desde el
primer mensaje; no telegráfico; no inventar URLs; verificar antes de afirmar.** El modelo de cuentas
ya está cerrado (arriba) — NO reabrirlo, solo ejecutar la reconexión.

## Desarrollo pendiente (cuando se cierre la infra)
- UI de **cuotas + excedentes + presupuesto** (backend de SEB-179 ya hecho y testeado, falta el frente).
- Fix `APP_DATABASE_URL` (se hace junto con la reconexión de Railway).
- CRIZA: deploy como proyecto en la cuenta de operación, cuando lo necesite.

## Nota de proceso (para Claude)
El desorden de cuentas Vercel/Railway nació de recomendaciones de identidad inconsistentes de Claude a lo largo de varias sesiones. El modelo de arriba es el cierre estable. **No reabrir el debate de identidad**: ejecutar.
