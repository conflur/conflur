# Sesión 2026-07-02 — Agente de Descubrimiento · Fase 3: canal web + setup staging

## 🔜 PRÓXIMA SESIÓN — retomar setup de staging + probar Fase 3 end-to-end

**El setup de staging quedó a 1 click de UI de terminar** — hay que abrir la próxima sesión con este contexto o se pierden 40 min re-descubriendo.

### Estado exacto al cierre (2026-07-02 fin de día)

**Git:**
- `main` local: tiene commits Fase 3 canal web (`f6bcc9a`) + docs staging (`c62cff5`). **NO pusheado** — pushear main dispara auto-deploy a staging, y staging todavía no build-ea (paso 1 pendiente).
- `production` local y en GitHub: apunta al commit `11ebdd0` = `3dae895` (prod pre-Fase 3) + docs staging cherry-pickeadas. Prod deployado desde acá.

**Neon:**
- Branch `main` (prod) — con datos, migr 0014 aplicada.
- Branch `staging` — creado con schema+data desde main. Endpoint `ep-red-recipe-at71sztx`. Rol `conflur_app` ya existe. Migr 0014 ya aplicada (venía en el snapshot).

**Railway (proyecto `conflur`, workspace Sebas-projects):**
- **`conflur-backend`** = prod. ✅ Online en `https://conflur-backend-production.up.railway.app`. Reconectado a GitHub, auto-deploy en rama `production`. Watcher confirmado ("⚡ Auto deploys when pushed to GitHub").
- **`conflur-backend-staging`** = creado hoy vía CLI (`railway add`). Service ID `499d7b11-7e20-4a97-999b-2e5f533eedcf`. Todas las env vars ya cargadas (ver abajo). ❌ Build **Failed** porque Railpack no encuentra cómo construir desde la raíz del repo. **Falta setear Root Directory=`backend`** — Railway CLI no expone flag para esto, es un click de UI.

**Vercel:**
- Proyecto `conflur` sigue watcheando `main` en rama de producción — **hay que cambiarlo a `production` ANTES de pushear main**, sino prod auto-deploya Fase 3 sin haberla testeado.
- Proyecto `conflur-staging` **no existe todavía** — crear después de que Vercel prod esté cambiado.

**Env vars ya cargadas en Railway staging (no re-cargar):**
| Variable | Valor |
|---|---|
| `APP_ENV` | `staging` |
| `DATABASE_URL` | `postgresql+asyncpg://neondb_owner:npg_QZY2VCIfS9ke@ep-red-recipe-at71sztx-pooler.c-9.us-east-1.aws.neon.tech/neondb` |
| `APP_DATABASE_URL` | `postgresql+asyncpg://conflur_app:npg_rgoy1J6DAURK@ep-red-recipe-at71sztx-pooler.c-9.us-east-1.aws.neon.tech/neondb` |
| `NEXTAUTH_SECRET` | `p9n7riLfLfwHpoR0k1DMTghmbK5+vCjpYdyf8eCfoK8=` (generado nuevo, distinto de prod) |
| `FRONTEND_URL` / `NEXTAUTH_URL` | `https://conflur-staging.vercel.app` **(placeholder — actualizar cuando Vercel staging exista)** |
| `ANTHROPIC_API_KEY`, `*_MODEL`, `KM_TENANT_ID`, `LOG_LEVEL` | copiados de prod |

### Pasos que faltan (en orden)

1. **[UI Railway]** `conflur-backend-staging` → Settings → Source → **Root Directory = `backend`** → Save. Railway redeploya automáticamente. Verificar deploy verde y `curl <staging-url>/health` → `{"status":"ok"}`. Anotar la URL pública que Railway asigna.
2. **[CLI]** Ejecutar `alembic upgrade head` contra el owner de Neon staging (idempotente — el branch ya trae 0014 pero confirma):
   ```
   cd backend && DATABASE_URL="postgresql://neondb_owner:npg_QZY2VCIfS9ke@ep-red-recipe-at71sztx-pooler.c-9.us-east-1.aws.neon.tech/neondb" python -m alembic upgrade head
   ```
3. **[UI Vercel]** Proyecto `conflur` → Settings → Git → Production Branch → cambiar `main` → `production`. Save. (Vercel puede rebuild-ear, no debería cambiar nada porque production apunta al mismo commit deployado hoy.)
4. **[UI Vercel]** Add New → Project → repo `conflur/conflur` → nombre `conflur-staging` → Root Directory `frontend` → Production Branch `main` → env vars:
   - `NEXT_PUBLIC_API_URL` = URL Railway staging (paso 1)
   - `NEXTAUTH_URL` = URL Vercel staging que Vercel te dé (se conoce después del deploy)
   - `NEXTAUTH_SECRET` = mismo que Railway staging (`p9n7riLfLfwHpoR0k1DMTghmbK5+vCjpYdyf8eCfoK8=`)
   - resto = mismos que prod
5. **[CLI Railway]** Actualizar en `conflur-backend-staging`: `FRONTEND_URL` y `NEXTAUTH_URL` = URL real de Vercel staging del paso 4. Redeploy.
6. **[CLI git]** `git push origin main` → Railway staging + Vercel staging auto-deployan Fase 3. Prod NO se toca.
7. **[Sebas]** Probar en staging: `/cuenta/discovery` → crear link → abrir link como psicóloga → chatear → cerrar → ver hallazgos.
8. Si pasa QA: `git checkout production && git merge --ff-only main && git push origin production && git checkout main` → prod deploya Fase 3.

### Fase 3b (Telegram) — sigue pendiente después de este deploy
Bot `python-telegram-bot` que usa el mismo motor de discovery. Link `t.me/conflur_bot` como alternativa al web.

### Bloqueadores conocidos / caveats
- **Railway CLI no expone root directory.** Único click de UI en todo el flujo. Documentado también en `docs/deploy-staging.md` §3.
- **`APP_ENV=development` y `NEXTAUTH_URL=http://localhost:3000` en prod actual** — bugs preexistentes de config, no tocarlos en esta sesión de deploy (fixear como tarea separada; no afecta Fase 3).
- **`ANTHROPIC_API_KEY` real** todavía sin setear (flag histórico ya listado en `agents.md`). El discovery web depende de esta key para responder en tiempo real. Si al probar el flujo la generación falla, revisar acá primero.

## Hecho hoy

### Canal web del Agente de Descubrimiento — completo

**Modelo + migración:**
- `DiscoverySession` (`backend/models/discovery_session.py`) — sin RLS: el UUID token (128 bits) es
  el control de acceso. FK a `tenants` (cascade) y a `discovery_findings` (al cierre). Campos:
  `nombre`, `referidor`, `history` (JSONB), `closed`, `finding_id`.
- Migración `0014_discovery_sessions` aplicada a Neon prod.

**Router `discovery/`** (`backend/discovery/router.py`):
- `POST /discovery/sessions` **(auth)** — crea sesión + genera apertura con el agente → devuelve
  `{token, url, nombre, history, closed}`. La URL lista para compartir.
- `GET /discovery/sessions/{token}` **(público)** — estado actual de la charla.
- `POST /discovery/sessions/{token}/message` **(público)** — agrega mensaje del usuario →
  `discovery_reply()` → guarda en history → devuelve `{reply, closed}`.
- `POST /discovery/sessions/{token}/close` **(público)** — `synthesize_discovery()` →
  `set_tenant()` → `save_finding()` (RLS via tenant_id de la sesión). Idempotente.
- `GET /discovery/findings` **(auth)** — sesiones del tenant + `aggregate_findings()` consolidado.

**Frontend:**
- `/discovery/[token]` — chat público (no necesita login). Burbujas agente/usuario, input,
  botón "Terminar conversación" (aparece al 6º mensaje). Pantalla de cierre con agradecimiento.
- `/cuenta/discovery` — dentro del producto (auth requerida). Formulario crear sesión
  (nombre + referidor) → muestra URL para copiar. Lista de charlas con estado + hallazgos
  consolidados (total, interesados, dolores frecuentes, contactos).

**Tipos en `frontend/src/lib/conflur.ts`:** `DiscoverySessionOut`, `DiscoverySessionSummary`,
`DiscoveryFindingsOut`, `createDiscoverySession`, `listDiscoveryFindings`.

**Tests:** 7 integration tests en `tests/test_discovery_router.py` — todos verdes.

**Bug corregido durante el proceso:** router llamaba `consolidate_findings(rows)` (async, toma
session) en lugar de `aggregate_findings(rows)` (sync, toma rows). Fix: cambio de import/call.

## Estado suite
- Unit: 23 tests ✅
- Integration: 7 discovery router + tests previos ✅
- Total suite completa: 77 tests ✅ (`77 passed, 16 warnings in 1401.43s`)

## Setup de staging (segunda mitad de la sesión)

Al ir a deployar Fase 3 surgió: **no había ambiente de staging**. Antes de mandar Fase 3 a prod
se decidió armar la separación local/staging/prod que quedó documentada en el playbook y en
`docs/architecture.md`.

### Diseño (nuevo, quedó en el repo)
- `docs/architecture.md` §Ambientes — tabla local/staging/prod + flujo git (`main` = staging,
  `production` = prod) + Decisión D-STAGING [2026-07-02].
- `docs/deploy-staging.md` — checklist de 7 pasos para armar staging por primera vez (Neon +
  Railway + Vercel + migraciones + verificación).

### Ejecución (hasta dónde llegué)
- **Git:** creada rama `production` apuntando a `3dae895` (commit prod pre-Fase 3), pusheada.
  Docs de staging cherry-pickeadas a ambas ramas. `main` local tiene Fase 3 + docs, **NO
  pusheada todavía** (se pushea recién cuando staging levante — sino Railway/Vercel prod
  auto-deployan Fase 3 sin QA).
- **Neon:** branch `staging` creado con schema+data desde `main`. Rol `conflur_app` ya presente.
  Migración 0014 aplicada (venía en el snapshot copiado).
- **Railway prod (`conflur-backend`):** reconectado a GitHub, ahora watchea rama `production`
  con auto-deploy activo. Verificado `/health` OK.
- **Railway staging (`conflur-backend-staging`):** servicio creado vía `railway add`, todas las
  env vars cargadas (incluidos overrides staging + Neon staging connection strings +
  `NEXTAUTH_SECRET` nuevo). ❌ **Build Failed** — Railway CLI no expone flag para setear
  Root Directory (`backend`), es un click de UI que quedó pendiente.

### Aprendizaje operativo
- **Railway "staged changes":** cambios de config (conectar repo, cambiar branch, etc.) quedan en
  modo "Apply N changes" hasta que se clickea Deploy — sin click no persisten. Perdimos ~40min
  antes de darnos cuenta. Documentado en memoria.
- **Railway CLI no cubre root directory** — sí crea servicios y setea vars, pero para monorepo
  con `railway.json` en subcarpeta hace falta ir a Settings → Source → Root Directory manualmente.

## Pendientes operativos
- Crear cycle en Linear para esta sesión.
- Los pasos operativos para terminar el deploy están en el bloque **🔜 PRÓXIMA SESIÓN** arriba.
